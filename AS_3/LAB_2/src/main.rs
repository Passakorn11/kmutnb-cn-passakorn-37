fn main() {
    let mut i = 0;
    let mut sum =  0;
    while i <= 100{
        sum = sum+i;
        i = i+1;
}
    println!("{}",sum)
    // กำหนดให้ i นับค่า1-100แล้วให้ตัวแปร sum เก็บค่าผลรวมของ i ก็จะได้ผลบวกตั้งแต่1-100
}
